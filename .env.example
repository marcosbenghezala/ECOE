# Copia este archivo a `.env` (NO lo commitees) para desarrollo local.
# En Railway, configura estas variables en el Dashboard.

# OpenAI
OPENAI_API_KEY=sk-proj-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Realtime API (WebSocket) - opcional
# Cambia el modelo si tu cuenta no tiene acceso al default.
OPENAI_REALTIME_MODEL=gpt-4o-realtime-preview-2024-12-17

# Timeouts/reintentos (para evitar timeouts en Railway/redes lentas)
OPENAI_REALTIME_OPEN_TIMEOUT=30
OPENAI_REALTIME_MAX_CONNECT_ATTEMPTS=3
OPENAI_REALTIME_CONNECT_BACKOFF_SECONDS=1.5

# Si quieres fijar el timeout del handshake end-to-end en `/ws/realtime/*`
# OPENAI_REALTIME_CONNECT_READY_TIMEOUT=100

# Proxy (opcional; dejar vacío si conectas directo a OpenAI)
# PROXY_URL=https://tu-proxy-ejemplo.up.railway.app

